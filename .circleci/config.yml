version: 2
jobs:
  build:
    docker:
      - image: python:latest
    steps:
      - checkout
      - run:
          name: create gazetteer
          command: python3 create_gazetteer_from_wikidata.py
      - run:
          name: run tests
          command: echo "will write testing code later"
